import { describe, expect, it } from 'vitest'

// Placeholder test confirming the Vitest runner and CI wiring work.
// Replace with real tests for parsing, baseline math, and rules in Phase 1+.
describe('test runner', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2)
  })
})
